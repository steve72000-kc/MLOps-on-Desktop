apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  # Leave injection labels unset so Prometheus can opt in via pod labels while
  # the rest of the namespace stays outside the mesh by default.
  annotations:
    argocd.argoproj.io/sync-wave: "5"
